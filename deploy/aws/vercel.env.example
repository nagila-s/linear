# Variaveis no painel Vercel (Production + Preview)

# Login do site
ACCESS_PASSWORD=

# BFF server-side (status, download, ISBN)
FASTAPI_URL=https://api.seudominio.com

# Upload direto no browser (mesma URL da API; evita limite ~4.5 MB da Vercel)
NEXT_PUBLIC_FASTAPI_URL=https://api.seudominio.com
NEXT_PUBLIC_API_PREFIX=/api/v1
